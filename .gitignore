.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# Third-party copyrighted journal articles — keep local only, do not publish.
literature_sources/

# Local Claude Code session config (machine-specific tool permissions).
.claude/

# LaTeX manuscript + build artifacts (paper/) — keep local only, not in the repo.
paper/

# OPC UA PKI store — generated certs + PRIVATE KEYS, never commit.
pki/
